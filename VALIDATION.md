# Validation

## Automated checks (passing)

- All 17 skills have valid YAML frontmatter with `name` and `description`.
- `tyf check` provides a deterministic, zero-token documentation-honesty check (skill count, name-matches-directory, dead skill IDs and command names, identical context files, valid JSON, em-dash discipline). It runs automatically warn-only after every mutating `tyf` command and hard-fails (exit 1) as a standalone command. Verified: clean pack passes (exit 0); introduced drift is caught both by the warn-only hook (command still succeeds) and by standalone `tyf check` (exit 1); `TYF_NO_DOC_HOOK=1` disables the hook.
- Count-consistency sweep: every stated skill count across the docs matches the actual number of skill directories (17). This check is itself the `keeping-documentation-honest` discipline applied to the pack.
- Every `name` matches its directory and is within 64 characters.
- Every `description` begins with "Use when", describes triggering conditions only, and is within 1024 characters.
- Every discipline skill (all but the dispatcher) carries a rationalization table and a red-flag list.
- No em-dashes in prose, headings, or frontmatter. The only retained em-dash is inside the canonical `[AUTHOR: needed — what]` sentinel token.
- All plugin and extension manifests are valid JSON.
- The `tyf` helper is exercised end to end in subprocess smoke tests: init, new-work, status, resume, start, import, adopt, open, mark-ready, proposal creation, enforced work-status transitions, source-fragment provenance, audit record plus inspectable Markdown audit note, author decision record with acceptance evidence, optional source line ranges for partial acceptance, exact accepted unified diffs for patch acceptance, sealed review-record integrity, per-unit manuscript write locks, write by decision, canonical event journaling, and doctor. Current smoke tests also cover `start` creating a titleless writing runway, updating root title/language metadata when supplied, creating first-session evidence under `sources/interviews/`, preserving a cold-start scaffold before drafting, and refusing the removed today-named command rather than keeping an alias; first-session packets are stored under `sources/interviews/` rather than `drafts/`; non-Latin titles and explicit writing-language metadata are preserved; UTF-8 manuscript text survives through the Gate; `begin` creates first-session packets without manuscript text; `import` preserves chat/zip arrivals under `sources/imports/`, updates active root title metadata when supplied, writes orientation packets, and performs no manuscript writes; cross-work reuse of workspace-owned source fragments, `capture` recording author source without touching `manuscript/`, `adopt` preserving direct author manuscript edits before the next write, `reflexes` explaining visible hooks, and `snapshot` creating an explicit git recovery commit scoped to the TYF workspace while leaving derived `.tyf/ledger.db` out of the commit. `doctor` was confirmed to flag a manuscript file that is not recorded in the write-log, to report tampered Gate review records, to report missing or changed source fragments referenced by Gate records, to report outstanding unit locks, to report missing accepted patches, to report missing, malformed, or tampered canonical event journals, and to exit non-zero on problems; mutating commands refuse to recreate a missing event journal over lost history. Re-run the same sequence in your actual target environment before relying on it; path and shell behavior can differ.

- An executable smoke-test suite (`tests/test_tyf.py`, stdlib `unittest`, zero dependencies) covers the helper's safety and integrity contract and is run with `python tests/test_tyf.py`: mutating commands refuse outside a workspace and refuse when the canonical event journal is untrustworthy; beta launch workspaces initialize as a single root work with `work.yaml`, `style-sheet.md`, `outline/`, `drafts/`, `manuscript/`, and `.review/`; work ids are sanitized and path-confined (absolute, `..`, separator ids, symlinked compatibility `works/`, symlinked work dirs, and symlinked manuscript dirs are rejected); `tyf start` opens a titleless root writing runway, records supplied title/language metadata, creates or reuses `sources/interviews/work-first-session.md`, preserves a cold-start scaffold through orientation, creates `drafts/candidate-draft.md`, never writes manuscript text, refuses the removed today-named command, and treats old positional-title start as an arrival path rather than a hidden compatibility alias while pointing mistaken title input to `tyf start --title`; `tyf begin` creates a safe first-session packet in `sources/interviews/` and refuses to clobber an existing work; `tyf import` preserves chat/text/zip/folder arrivals under `sources/imports/`, preserves binary/unreadable/oversized arrivals with `Extraction needed` guidance instead of pretending they were read, updates active root title/language metadata when supplied, creates orientation packets, lists and contains folder or TYF-shaped zip arrivals before merge, mints fragments for textual arrivals, and never writes manuscript text; `tyf status` reports active work metadata and status from `work.yaml` as well as workspace state; `tyf resume` reports active work continuity and next useful move; `tyf capture` requires an existing work, records author material outside `manuscript/`, and mints source fragments for source captures; `tyf attend` writes source-grounded `.review/gentle-attention.md` packets without manuscript text, can focus on one source fragment, and refuses missing, unsafe, or tampered source refs without writing a packet; `tyf feedback` requires exactly one body source, preserves external critique under `sources/feedback/`, writes review-only `.review/feedback/` triage packets, treats critique as reader experience rather than authority or command text, supports UTF-8 feedback files, and writes no manuscript text; author-facing skill surfaces avoid private development context; `tyf character` and `tyf consult-character` keep per-character knowledge and voice isolated, support UTF-8 names, refuse missing dossiers, and generate review-only packets that let a host sub-agent return candidate lines only through the amanuensis; source fragments are workspace-owned and reusable across works while retaining origin metadata; `tyf adopt` preserves direct author manuscript edits and updates the logged base for the next decision; `tyf reflexes` exposes hooks and snapshot behavior; `tyf snapshot` requires git and commits only the TYF workspace path; `.tyf/events.jsonl` records core apparatus actions as a hash-chained canonical journal that `tyf doctor` verifies; the Gate chain refuses naked confirmation, requires acceptance evidence, advances work status through proposal, audit, acceptance, and write, refuses acceptance before audited state, refuses acceptance after failed audit state, refuses acceptance using another proposal's audit, and refuses write before accepted state, carries source refs from proposal through audit, decision, and write-log, writes only from a proposal plus passing audit plus decision record, writes inspectable Markdown audit notes beside audit JSON, supports strictly increasing non-overlapping source line ranges for partial acceptance, supports exact reviewed unified diffs with `tyf accept --patch`, rejects malformed patch hunk counts, verifies source, accepted patch, and base hashes, rejects replayed decisions, refuses tampered proposal/audit/decision records through `.review/record-seals.jsonl`, acquires a per-unit lock under `.review/locks/`, and records proposal, decision, audit, source refs, accepted scope, and content hash; `tyf doctor` detects an out-of-band edit to a logged manuscript file by hash, missing or mismatched Gate record seals, missing or changed source fragments, missing or changed accepted patches, missing/malformed/tampered event journals, and outstanding unit locks; source provenance refuses file-only tampering and index-plus-file rewrites under the old fragment id; `tyf notice` suppresses style-sheet-lag after clean controlled writes but still reports unlogged manuscript changes; `tyf check` flags stale role terminology, stale ledger paths, stale writing-runway routing, plugin manifest version divergence, and stale release evidence counts while accepting clean `author-context/` templates in exported author packages; the installers target Codex's current skill root, route book workspaces away from contributor context, keep installed-helper `tyf init` quiet when the full pack is unavailable, and include a Windows PowerShell path that installs Codex skills plus `tyf.cmd`/`tyf.ps1` launchers with `TYF_PACK_ROOT`; release export rules exclude development control directories, caches, and pyc debris, exported author trees pass `tyf check`, a bash installer smoke test installs skills plus the helper from the exported tree, and an exported Codex install can initialize a separate book workspace, preserve an arrival scaffold, open the writing runway, and keep `manuscript/` empty; and `_pack_root` honors `TYF_PACK_ROOT`. The `bin/tyf` launcher is exercised end to end. Current suite size: 154 tests.
- The hidden development acceptance gate is not an author dependency. Its immediate-feature round now has 117 scenarios covering the helper contract, writing runway, single-work beta launch, amanuensis entry and continuity, source-grounded attention packets, external-feedback triage, character-contained consultation, import containment including unreadable and oversized arrivals, direct-edit adoption, portable workspace markers, documentation command-drift and pressure-status checks, clean author-context packaging, private-context-free author surfaces, release export hygiene, Windows PowerShell install, source-fragment provenance through the Gate, enforced work-status transitions, sealed Gate record integrity, per-unit write locking, partial Gate acceptance, exact accepted patch application, malformed accepted patch refusal, canonical event journaling and mutation refusal when history is missing, explicit writing-language metadata and UTF-8 text, public onboarding, Codex plugin validity, Codex book-repo skill surface including a fresh exported install-to-book start, title-footgun recovery guidance, installed-helper first-run quietness, and attentive notice quality after controlled writes. All 117/117 current scenarios have direct RED proof recorded. Independent structural oracles add helper, writing-runway, single-work, amanuensis-entry, character-consultation, feedback-triage, portability, onboarding, Gate, provenance, plugin, and Codex-skill checks beyond the broad helper unittest suite and the Codex plugin validator.
- De-ceremonialized: the parallel ceremonial register is removed. Skill IDs, titles, prose, and the helper are plain and functional. Skills `offering-sources`, `conducting-sittings`, `building-the-field`, `keeping-voice-ledger`, `reading-as-first-reader`, `diagnosing-with-loupe`, `auditing-with-devils-reader`, and `operating-the-gate` were renamed; the Gate concept is now the concrete proposal, audit, decision, and write-decision chain; `source-vault`/`voice-ledger`/`claims-ledger` became `sources`/`voice`/`claims`. `redactor` and `amanuensis` are kept as precise terms.
- `tyf notice` is the deterministic attentive-amanuensis loop (surfaces gaps, unfinished lines, unsourced claims, uncontrolled or unresolved style-sheet lag, unused registers; modifies nothing; zero tokens). `tyf reconcile` lists previously saved items. Verified end to end against a workspace with planted issues: all five detector kinds fired, `--save` wrote a dated digest to `.proposals/notices.md`, a clean controlled write no longer creates style-sheet lag by mtime alone when the manuscript hash matches the write-log and the style sheet has core metadata, and a write surfaced an outstanding-count reminder without blocking the write. The semantic Learn pass in `docs/LEARN_PASS.md` is opt-in and unwired: nothing reads the corpus or spends tokens in the background until the author enables it.
- The attentive loop is ledger-backed (`.tyf/ledger.db`), content-addressed rather than timestamp-based, so it works without git. Verified end to end: a new item surfaces once, stays quiet on an unchanged re-run, is silenced by `tyf dismiss <hash>`, and resurfaces when its surrounding context changes (a dismissed gap returned once a contradicting line was added nearby). Removed content auto-resolves. The loop never ranks which of two authored statements wins; contradictions are surfaced for the author. Per-run and per-write capture are wired; per-message capture is documented opt-in in `docs/ATTENTIVENESS.md`.
- Apparatus memory uses `.tyf/events.jsonl` for the canonical hash-chained event journal and `.tyf/ledger.db` (stdlib `sqlite3`, no new dependency) for the derived content-addressed notice index. The body of work stays in Markdown/YAML. Verified: idempotent `tyf init` (re-run is a no-op; deleting a dir or file and re-running heals exactly the missing pieces), notice dedupe within a run, dismiss, resurface-on-context-change, `tyf reconcile --export` Markdown mirror, the event spine, and `tyf doctor --repair` restoring deleted structure.
- Product-lens review added: `tests/acceptance-and-edge-cases.md` gives acceptance criteria and 2 to 5 break/edge cases per skill (empty, adversarial, ambiguous, contradictory, missing-precondition, injection). The thinnest-guarded and highest-stakes skills (reading-sympathetically, controlling-manuscript-writes, ingesting-sources, structuring-knowledge, editing-faithfully, auditing-adversarially) gained an in-skill `Acceptance and edge cases` section. Three new RED/GREEN scenarios (source injection, offline citation index, out-of-band manuscript edit) added to `pressure-scenarios.md`. The out-of-band-edit case is enforced deterministically by `tyf doctor` via a per-write content hash recorded in the write-log, and is covered by `tests/test_tyf.py` (verified).

## Manual checks recommended before publishing

- **Subagent pressure testing.** Run `tests/pressure-scenarios.md` in your harness: once with skills absent (RED) and once present (GREEN). Add any new rationalization that slips through to the relevant skill's table and re-run.
- **Cowork install.** Confirm the skills load in a Cowork project, the project instructions paste cleanly, `/schedule` accepts the templates in `cowork/SCHEDULED_TASKS.md`, and the controlled write holds (no manuscript write appears without a write-log entry; `tyf doctor` catches it if one does).
- **Manifest schemas.** Validate each harness's plugin or extension manifest against that harness's current plugin docs before publishing. See the caveat in `docs/PORTABILITY.md`.
- **MCP citation index.** Citation verification in `auditing-adversarially` assumes an MCP citation index (Zotero or Scite) is connected. Confirm it is available, or treat verification findings as manual.

## Comparison and known gaps vs superpowers

`docs/COMPARISON_SUPERPOWERS.md` benchmarks TYF against `obra/superpowers`. Named gaps TYF does not yet address: (1) prompt-level RED/GREEN pressure scenarios have had only a first subagent run (`tests/PRESSURE_RESULTS.md`): GREEN passed 11 of 11, though the RED baseline is weak on a strong base model, so that proof remains partial even though the hidden command-backed development scenarios have 117/117 direct RED proof; (2) no execution-to-completion skill (superpowers' executing-plans); (3) no debugging/isolation skill (systematic-debugging); (4) receiving-critique now exists with command-backed triage, but has little real editor/beta-reader usage; (5) no auto-update distribution path beyond notify-only `tyf update`; (6) no parallelism story. These are roadmap, not regressions.

## Known scope decisions

- Concurrent manuscript writes are guarded by manuscript base hashes and per-unit lock files during the controlled write mutation. TYF does not yet attempt stale-lock auto-repair; `tyf doctor` surfaces outstanding locks for human reconciliation.
- 17 skills: 4 lifecycle, 11 editorial-apparatus, 2 cross-cutting substrates (voice registers, redactor canon).
- The redactor (Milchin) discipline runs as a substrate and threads through the diagnosis pass, the editor, and the adversarial audit at micro, macro, and meta.
- The three knowledge-band disciplines (thesis interrogation, argument spine, claims index) remain embedded in `interviewing-the-author`, `structuring-knowledge`, and `auditing-adversarially`; promoting them to their own cells remains later roadmap work.
