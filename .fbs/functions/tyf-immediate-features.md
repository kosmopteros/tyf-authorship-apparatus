# TYF immediate writing features

**Purpose:** make the TYF authorship apparatus usable for starting a new book today while keeping the apparatus visible, recoverable, and provenance-safe.

**Stakeholders:** Alexander, Pegasus/Codex, Claude Cowork, and future helpers entering the same writing workspace.

**Parent:** root

## Claims

- `tyf-helper-current-contract`: the TYF helper smoke suite exercises workspace safety, a plain-language first-session start path, explicit writing-language metadata and UTF-8 manuscript text, provenance-safe capture, visible reflexes, explicit git snapshots, canonical event journaling, Gate-controlled writes, enforced work-status transitions, sealed review-record integrity, manuscript-unit write locking, partial source-line acceptance, exact accepted patch application, documentation honesty, and update checks without writing manuscript text outside proposal, audit, author decision with acceptance evidence, optional accepted line ranges or accepted unified diffs, and `tyf write --decision` records {formulated_by=req:d6e294d5;req:b2b3df90;req:012517f3;req:e4214bd5;req:3ff0dc15;req:c52b3a3e;req:3f7a618;req:a4629e7;req:ae34c916, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for first-day writing setup}
- `tyf-source-provenance-contract`: source captures mint stable source fragment files and an index entry, proposals can bind those fragments with `--source-ref`, audit and author-decision records preserve the source refs, controlled writes log them, and both write and doctor refuse missing or changed fragment files {formulated_by=req:3b338964, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for source-fragment integrity checks}
- `tyf-patch-acceptance-contract`: author decisions can accept exact reviewed unified diff files under `.review/`, decision records store the patch path, hash, unit, and accepted scope, controlled writes apply only that patch against the recorded manuscript base, and write/doctor refuse missing or changed accepted patch files {formulated_by=req:3f7a618, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for patch application}
- `tyf-canonical-event-journal-contract`: apparatus actions are recorded in `.tyf/events.jsonl` as a human-readable hash chain, SQLite keeps only a derived event mirror for reconciliation counts, `tyf doctor` detects a missing, malformed, or tampered canonical journal, and mutating commands refuse before touching workspace files when the journal is not trustworthy {formulated_by=req:a4629e7, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for event journal validation}
- `tyf-work-state-machine-contract`: work status is an enforced Gate state machine: a new work starts `structuring`, `mark-ready` moves it to `ready-for-audit`, proposal moves it to `drafting`, passing audit moves it to `audited`, failing audit moves it to `needs-revision`, author acceptance moves it to `accepted`, controlled write moves it to `written`, accept/write refuse when the recorded state is not at the required transition point, and author acceptance requires a passing audit for the same proposal hash rather than any work-wide audited status {formulated_by=req:ae34c916, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for status transition checks}
- `tyf-amanuensis-entry-contract`: TYF provides an amanuensis-centered entry and continuity slice: `tyf start` can begin without a title and records unknown title/language as open state, first-session evidence lives in `sources/interviews/` rather than drafts, `tyf import` preserves chat/text/zip/folder arrivals under `sources/imports/` with orientation packets and no manuscript writes, zip/folder arrivals are containment-first with analysis guidance before merge, source fragments are workspace-owned and cross-work reusable, fresh intake prompts do not surface as overdue notice gaps, `tyf resume` reports the active work and next useful move, and `tyf adopt` records direct author manuscript edits as the new base before later controlled writes {formulated_by=req:681a56b1;req:d20bb74c;req:ae866796;req:593bffcb;req:45e26aea;req:ce3753e2;req:a145ae89;req:2bafd906;req:c49911d7;req:18b23109;req:ec899386;req:0cae885f, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local entrypoint commands}
- `tyf-portable-workspace-contract`: TYF workspaces include a `tyf.portable.json` marker that identifies the bundle format, canonical text state in Markdown/YAML/JSONL including `.tyf/events.jsonl`, derived disposable SQLite notice state, and git as optional, so agents can recognize and re-enter a moved, zipped, or handed-off author workspace without treating SQLite or git as prose custody {formulated_by=req:d31be9f7, axes=bad-outcome;edge;boundary;integration, na=security:local marker and documentation only;non-functional:no throughput or latency budget for marker reads}
- `tyf-doc-drift-command-contract`: TYF's deterministic documentation check flags retired public commands and stale apparatus-memory paths, so public docs and routing surfaces cannot keep telling agents to use `tyf write --confirm` or old ledger locations after the runtime changes {formulated_by=req:017f189c, axes=bad-outcome;edge;integration, na=security:local documentation scan over repository files;non-functional:no throughput or latency budget for documentation checks}
- `tyf-public-onboarding-contract`: the public TYF onboarding surface gives non-technical authors paste-ready Codex and Claude Cowork prompts, routes agents through `using-tyf` and `tyf start`, and keeps advanced helper commands behind agent-facing skill guidance rather than presenting them as the author's first task {formulated_by=req:d2077fb4, axes=bad-outcome;edge;boundary;integration, na=security:local documentation and skill routing with no secret or auth boundary;non-functional:no throughput or latency budget for onboarding text}
- `tyf-codex-plugin-valid`: the Codex plugin manifest validates against the current local Codex plugin validator, including skills path, author object, interface metadata, and skill frontmatter YAML {formulated_by=req:d2077fb4, axes=bad-outcome;edge;boundary;integration, na=security:local plugin metadata has no runtime permission grant or secret-handling surface;non-functional:no performance budget for manifest validation}
