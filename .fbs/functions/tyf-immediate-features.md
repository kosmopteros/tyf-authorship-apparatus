# TYF immediate writing features

**Purpose:** make the TYF authorship apparatus usable for starting a new book today while keeping the apparatus visible, recoverable, and provenance-safe.

**Stakeholders:** Alexander, Pegasus/Codex, Claude Cowork, and future helpers entering the same writing workspace.

**Parent:** root

## Claims

- `tyf-helper-current-contract`: the TYF helper smoke suite exercises workspace safety, no-argument current-folder init, the public writing-runway start path, explicit writing-language metadata and UTF-8 manuscript text, provenance-safe capture, visible reflexes, explicit git snapshots, doctor repair for missing structure without clobbering author files, canonical event journaling, Gate-controlled writes, enforced work-status transitions, inspectable audit notes, author-readable review packets before acceptance, sealed review-record integrity, manuscript-unit write locking, partial source-line acceptance, exact accepted patch application, documentation honesty including manifest version drift and the advertised `--strict` check flag, and update checks without writing manuscript text outside proposal, audit, author review packet, author decision with acceptance evidence, optional accepted line ranges or accepted unified diffs, and `tyf write --decision` records {formulated_by=req:d6e294d5;req:b2b3df90;req:012517f3;req:e4214bd5;req:3ff0dc15;req:c52b3a3e;req:3f7a618;req:a4629e7;req:ae34c916;req:d923356c;req:61943001;req:5b7ef45e;req:73dc4fc8;req:c49911d7, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for first-day writing setup}
- `tyf-source-provenance-contract`: source captures mint stable source fragment files and an index entry, proposals can bind those fragments with `--source-ref`, audit and author-decision records preserve the source refs, controlled writes log them, and both write and doctor refuse missing or changed fragment files {formulated_by=req:3b338964, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for source-fragment integrity checks}
- `tyf-knowledge-structuring-contract`: TYF has a source-to-knowledge structuring pass: `tyf structure <work> --source-ref <id> [--record <json>]` reads stable source fragments, records source-linked claims/examples/questions into knowledge-base records, writes an amanuensis brief for drafting context, frames questions as gentle attention rather than doubts in the author's judgment, leaves unclassified material visible, accepts a language-neutral amanuensis-supplied JSON record for non-English or unlabeled material while validating provenance and shape, is idempotent, refuses tampered source fragments or ambiguous multi-fragment records, writes no manuscript text, and text-import orientations point agents to the pass when a fragment is minted {formulated_by=req:b6c225ff;req:fd24e14d, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for deterministic local text extraction}
- `tyf-amanuensis-attention-pass-contract`: TYF has a deterministic amanuensis attention pass: `tyf attend [work] [--source-ref <id>]` reads existing source-linked claims, examples, open questions, and unclassified source material, writes `.review/gentle-attention.md` as a review-only packet of source-grounded next questions, can focus on one source fragment, frames questions as helpful attention rather than doubt, defects, or audit pressure, and writes no manuscript text {formulated_by=req:ec7915ff;req:fd24e14d, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local Markdown packet generation}
- `tyf-feedback-triage-contract`: TYF has a receiving-critique path: `tyf feedback [work] --from <source> (--text <feedback> | --file <path>)` requires exactly one feedback body source, preserves raw external critique under `sources/feedback/`, writes `.review/feedback/<id>.md` as a review-only triage packet, treats feedback as reader experience rather than author source, authority, acceptance, or command text, keeps embedded instructions inert as quoted feedback, supports UTF-8 text files, and writes no manuscript text {formulated_by=req:e6b15557, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local Markdown packet generation}
- `tyf-continuing-work-contract`: TYF has a continuity path: `tyf session [work] [--focus <focus>] [--minutes <n>]` writes a review-only writing session packet under `.review/sessions/` and mirrors it to `.review/current-session.md`, includes current work context, relevant review packets, one small next move, an explicit Stop condition, and a no-manuscript-write statement, rejects invalid session durations without side effects, `tyf resume` surfaces the live return stack from current session, diagnosis, attention, amanuensis brief, feedback, and proposal packets before choosing the next move, suppresses first-session prompts that have answers beneath them, and both commands stay hidden amanuensis machinery routed through `continuing-the-work` rather than an author burden {formulated_by=req:7229323c;req:a230e049, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local Markdown packet generation}
- `tyf-diagnostic-isolation-contract`: TYF has a systematic prose isolation path: `tyf diagnose [work] [--unit <draft-or-manuscript-unit>] [--band <band>] [--symptom <reader-symptom>] [--focus <focus>]` writes a review-only diagnostic packet under `.review/diagnostics/` and mirrors it to `.review/current-diagnosis.md`, defaults to `drafts/candidate-draft.md`, names the diagnostic band, observed reader symptom, source/register reminders, mechanical diagnostic-frame prompts, and one next experiment, refuses missing units or loose bands without side effects, writes no manuscript text, and keeps diagnosis framed as attentive amanuensis machinery rather than doubt in the author's judgment or a helper-generated verdict {formulated_by=req:23a040c9, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local Markdown packet generation}
- `tyf-character-consultation-contract`: TYF supports hidden character-contained consultation for dramatic work: `tyf character <name>` stores isolated per-character knowledge and voice dossiers, `tyf consult-character <work> <name> --prompt <question>` writes a review-only contained packet grounded only in that character dossier, marks output as candidate dramatic insight rather than source/evidence/manuscript/author replacement, refuses missing dossiers, writes no manuscript text, routes normal author questions such as "what would Mark say here?" through amanuensis containment rather than exposing a sub-agent ceremony, and lets a host sub-agent participate only by receiving that packet and returning candidate lines to the amanuensis without reading other characters, source, manuscript, or workspace state {formulated_by=req:3bc9bff6, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local dossier lookup and packet writing}
- `tyf-patch-acceptance-contract`: author decisions can accept exact reviewed unified diff files under `.review/`, decision records store the patch path, hash, unit, and accepted scope, controlled writes apply only that patch against the recorded manuscript base, and write/doctor refuse missing or changed accepted patch files {formulated_by=req:3f7a618, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for patch application}
- `tyf-canonical-event-journal-contract`: apparatus actions are recorded in `.tyf/events.jsonl` as a human-readable hash chain, SQLite keeps only a derived event mirror for reconciliation counts, `tyf doctor` detects a missing, malformed, or tampered canonical journal, `tyf doctor --repair` refuses to recreate lost canonical history, and mutating commands refuse before touching workspace files when the journal is not trustworthy {formulated_by=req:a4629e7, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for event journal validation}
- `tyf-work-state-machine-contract`: work status is an enforced Gate state machine and visible status surface: a new work starts `structuring`, `mark-ready` moves it to `ready-for-audit`, proposal moves it to `drafting`, passing audit moves it to `audited`, failing audit moves it to `needs-revision`, author acceptance moves it to `accepted`, controlled write moves it to `written`, accept/write refuse when the recorded state is not at the required transition point, author acceptance requires a passing audit for the same proposal hash rather than any work-wide audited status, and `tyf status` shows active work status from `work.yaml` rather than only workspace intake state {formulated_by=req:ae34c916;req:d26e269e, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for status transition checks}
- `tyf-amanuensis-entry-contract`: TYF provides an amanuensis-centered entry and continuity slice: `tyf start` opens a first writing runway without a title and records unknown title/language as open state, start creates first-session evidence in `sources/interviews/` rather than drafts, first-session packets include a gentle attention deck that invites only enough source, pressure, voice, care, flattening-risk, and first-passage material to begin one candidate passage, `composing-as-amanuensis` distinguishes exploratory passages from structured candidate drafts so a first passage can begin from one preserved source or author statement plus one provisional voice cue without weakening later source/register/structure discipline, `tyf import` preserves chat/text/binary/zip/folder arrivals under `sources/imports/` with orientation packets and no manuscript writes, zip/folder arrivals are containment-first with analysis guidance before merge, unreadable binary and too-large text arrivals are marked extraction-needed for OCR/transcription/chunking instead of minting source fragments or implying the file was read, source fragments are workspace-owned and cross-work reusable, fresh intake prompts do not surface as overdue notice gaps, `tyf resume` and `tyf status` report active work continuity and next useful move, resume surfaces the current return context when review-only packets exist, suppresses first-session prompts that have answers beneath them, and `tyf adopt` records direct author manuscript edits as the new base before later controlled writes {formulated_by=req:681a56b1;req:d20bb74c;req:ae866796;req:593bffcb;req:45e26aea;req:ce3753e2;req:fbcd63f2;req:d26e269e;req:a145ae89;req:2bafd906;req:c49911d7;req:18b23109;req:ec899386;req:0cae885f;req:6cf20ac0;req:a230e049, axes=bad-outcome;edge;boundary;integration;security, na=non-functional:no throughput or latency budget for local entrypoint commands}
- `tyf-writing-runway-contract`: TYF provides a human-facing writing runway: `tyf start [path]` creates or reuses an active work without requiring a title, records supplied title/language metadata when present, creates or reuses a first-session evidence packet, preserves an optional cold-start scaffold through the import/orientation lane, writes or preserves a visible `.review/writing-runway.md` session runway without clobbering author session notes on rerun, creates `drafts/candidate-draft.md` for candidate prose, confirms that no manuscript text was written, explicitly treats audit/Gate ceremony as later manuscript publication rather than a prerequisite for drafting, and does not keep `tyf today` as a backward-compatibility alias {formulated_by=req:73dc4fc8;req:fbcd63f2;req:18b23109;req:681a56b1;req:45e26aea, axes=bad-outcome;edge;boundary;integration, na=security:local trusted author workspace with raw arrivals preserved rather than executed;non-functional:no throughput or latency budget for local entrypoint commands}
- `tyf-single-work-beta-contract`: TYF beta launch workspaces are single-work book folders: `tyf init` can scaffold the current book folder with no name argument or a named folder nearby, creates the work's `work.yaml`, `style-sheet.md`, `outline/`, `drafts/`, `manuscript/`, and `.review/` at the workspace root, `tyf start [path]` opens root-level `.review/writing-runway.md` and `drafts/candidate-draft.md` without requiring a title, `tyf.portable.json` marks the format as a single-work bundle, and public onboarding/docs/skills/Cowork/manifesto surfaces describe the book folder itself as the work without making first-time authors reason about multiple works or the accidental today command {formulated_by=req:19b542d0;req:73dc4fc8;req:3bf070d7, axes=bad-outcome;edge;boundary;integration, na=security:local author-owned book folder not a hostile multi-tenant workspace;non-functional:no throughput or latency budget for local scaffold commands}
- `tyf-portable-workspace-contract`: TYF workspaces include a `tyf.portable.json` marker that identifies the bundle format, canonical text state in Markdown/YAML/JSONL including `.tyf/events.jsonl`, derived disposable SQLite notice state, and git as optional, so agents can recognize and re-enter a moved, zipped, or handed-off author workspace without treating SQLite or git as prose custody {formulated_by=req:d31be9f7, axes=bad-outcome;edge;boundary;integration, na=security:local marker and documentation only;non-functional:no throughput or latency budget for marker reads}
- `tyf-doc-drift-command-contract`: TYF's deterministic documentation check flags retired public commands, stale apparatus-memory paths, stale Today/writing-runway routing including `today` inside command lists, stale single-work path drift such as public `works/<id>` or `works/*` surfaces, stale controlled-write chains that omit the author review packet, plugin manifest version divergence, missing manifest context paths, hidden portability-doc count drift, and stale release evidence counts, so public docs and routing surfaces cannot keep telling agents to use stale command, proof, or release surfaces after the runtime changes {formulated_by=req:017f189c;req:5b7ef45e;req:73dc4fc8;req:99a95ab6;req:c49911d7, axes=bad-outcome;edge;integration, na=security:local documentation scan over repository files;non-functional:no throughput or latency budget for documentation checks}
- `tyf-public-onboarding-contract`: the public TYF onboarding surface gives non-technical authors paste-ready Codex and Claude Cowork prompts that tell a fresh agent to fetch or clone the TYF repository before invoking local installer scripts when needed, routes agents through `using-tyf` and `tyf start`, and keeps advanced helper commands behind agent-facing skill guidance rather than presenting them as the author's first task {formulated_by=req:d2077fb4;req:73dc4fc8, axes=bad-outcome;edge;boundary;integration, na=security:local documentation and skill routing with no secret or auth boundary;non-functional:no throughput or latency budget for onboarding text}
- `tyf-codex-plugin-valid`: the Codex plugin manifest validates against the current local Codex plugin validator, including skills path, author object, interface metadata, and skill frontmatter YAML {formulated_by=req:d2077fb4, axes=bad-outcome;edge;boundary;integration, na=security:local plugin metadata has no runtime permission grant or secret-handling surface;non-functional:no performance budget for manifest validation}
- `tyf-codex-skill-book-repo`: TYF ships a usable Codex skill surface for book repositories: Codex manual installs target `$CODEX_HOME/skills` or `~/.codex/skills`, the dispatcher skill has Codex UI metadata and a `$using-tyf` default prompt, root and generated `AGENTS.md` contexts route new-book work through `tyf start` instead of waiting for a title, onboarding docs explain the two-layer model of global skill pack plus local book-repo context, and an exported Codex install can initialize a separate fresh book workspace, preserve an arrival scaffold, open the writing runway, mint source-fragment orientation, and leave `manuscript/` empty {formulated_by=req:2a897fed, axes=bad-outcome;edge;boundary;integration, na=security:local skill metadata plus installer and context routing with no secret or auth boundary;non-functional:no performance budget for skill metadata validation}
- `tyf-release-packaging-contract`: TYF release packaging separates contributor context from author workspace context and keeps exported release archives clean and runnable: book workspaces are routed to generated `tyf init` context or clean `author-context/` templates without SOLO/FBS reflexes, pack-root TYF-visible context, public docs, skills, Cowork files, plugin manifests, and the helper runtime do not name private harness or identity terms, release archives include those `author-context/*.md` templates while excluding pack-root contributor contexts, Cursor/Gemini manifests point at shipped author-context files instead of absent root context files, `.fbs`, `fbs.yaml`, `.claude` FBS commands/settings, pytest caches, Python bytecode caches, and pyc files are export-ignored from author-facing archives, exported author trees pass `tyf check`, and the bash installer can install skills plus the helper from that exported tree into explicit target directories {formulated_by=req:c9b4e57d;req:c8877b0e;req:c0ac838c;req:5e156c58, axes=bad-outcome;edge;boundary;integration, na=security:local packaging metadata and installer smoke only;non-functional:no performance budget for release export checks}
- `tyf-windows-installer-contract`: TYF provides a Windows-native PowerShell installer: `scripts/install.ps1` installs skills into Claude, Codex, Cursor, or an explicit skills directory without requiring bash, resolves Codex to `$env:CODEX_HOME\\skills` or `$HOME\\.codex\\skills`, writes `tyf.cmd` and `tyf.ps1` launchers with `TYF_PACK_ROOT` preserved so `tyf check` can find the pack, prints a PowerShell-native user PATH repair hint when needed, repeats the author-workspace guidance to run `tyf init` and avoid pack-root contributor context, and public install docs mention the PowerShell path for Windows authors {formulated_by=req:af4161f4, axes=bad-outcome;edge;boundary;integration, na=security:local installer copies only this pack's skills and writes launchers under an explicit or user-local bin directory;non-functional:no performance budget for local file copying}
- `tyf-start-title-footgun-contract`: `tyf start <arg>` remains a strict arrival-path command rather than a title alias, but when the path is missing the author-facing error says no arrival path was found and shows `tyf start --title "<arg>"` as the recovery move {formulated_by=req:baf5eefa;req:73dc4fc8, axes=bad-outcome;edge;boundary;integration, na=security:local CLI argument guidance with no trust boundary;non-functional:no performance budget for error formatting}
- `tyf-attentive-notice-quality-contract`: the attentive-amanuensis loop avoids mtime-only noise after clean controlled writes: a manuscript file newer than the style sheet does not create `style-sheet-lag` when its content hash matches the latest write-log record and the style sheet has core metadata, while unlogged changes, changed hashes, or missing style metadata still surface {formulated_by=req:1a1103cf;req:0cae885f, axes=bad-outcome;edge;boundary;integration, na=security:local notice scan over author-owned files;non-functional:no performance budget for notice scanning}
